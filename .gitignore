node_modules/
dist/
coverage/

# Bí mật: hash mật khẩu và JWT_SECRET. Dùng .env.example làm mẫu.
.env
.env.*
!.env.example

# Dữ liệu tài chính thật — không bao giờ đưa lên remote.
*.db
*.db-journal
*.db-wal
*.db-shm
backups/

# Prisma Client sinh ra lúc build, chạy `npx prisma generate` trên server.
src/generated/

# Rác do các công cụ AI tạo ra
.claude/
.windsurf/
.agents/
skills-lock.json

*.log
.DS_Store
